name: Update Gists

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    uses: prjctimg/profile/.github/workflows/update.yml@main
    with:
      commit-message: "sketching my life force away..."
      git-user-name: "skchr"
      git-user-email: "prjctimg@yandex.com"
    secrets: inherit
